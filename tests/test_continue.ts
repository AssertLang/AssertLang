export function count_positives(numbers: Array<number>): number {
  const count = 0;
  for (const num of numbers) {
    if ((num < 0)) {
      // Unknown statement: NoneType
    }
    count = (count + 1);
  }
  return count;
}
