// Test 3: Continue
function count_positives(numbers: array<int>) -> int {
    let count = 0;
    for (num in numbers) {
        if (num < 0) {
            continue;
        }
        count = count + 1;
    }
    return count;
}
