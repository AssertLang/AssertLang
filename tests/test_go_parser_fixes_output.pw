module main
version 1.0.0

import fmt
import math/rand

let SIZE = 15
let MEMORY_FILE = "memory.json"
let START = [0, 0]
let END = [(SIZE - 1), (SIZE - 1)]

function MakeMaze:
  params:
    size int
  returns:
    result array<array<int>>
  body:
    let maze = lambda : ...
    let result = [][]int{}
    for i in range:
    let row = []
    for i in range:
    let cell = lambda : ...
    if rand.Float64():
    return 1
    return 0
    row = append(row, cell)
    result = append(result, row)
    return result
    let unknown = null
    let unknown = null
    return maze

function main:
  body:
    fmt.Println("Testing closures and module vars")
    let maze = MakeMaze(SIZE)
    fmt.Printf("Generated %dx%d maze\\n", len(maze)
