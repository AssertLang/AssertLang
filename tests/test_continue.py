from __future__ import annotations

from typing import List

from __future__ import annotations

def count_positives(numbers: List[int]) -> int:
    count = 0
    for num in numbers:
        if (num < 0):
            # Unknown statement: NoneType
        count = (count + 1)
    return count
