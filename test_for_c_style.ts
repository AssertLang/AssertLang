export function count(): number {
  const x = 0;
  for (const i = 0;; (i < 10); i = (i + 1);) {
    x = (x + 1);
  }
  return x;
}

export function sum_range(n: number): number {
  const total = 0;
  for (const i = 0;; (i < n); i = (i + 1);) {
    total = (total + i);
  }
  return total;
}
