#lang javascript

function main() -> int {
    let text = "hello world";
    let words = str.split(text, " ");
    print("JavaScript says:", words);
    
    let upper = str.upper(text);
    print("Uppercase:", upper);
    
    return 0;
}
